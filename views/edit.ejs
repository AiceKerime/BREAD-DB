<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="/css/style.css" rel="stylesheet" type="text/css">

    <title>Document</title>
</head>

<body>
    <h2>Edit Data</h2>

    <form id="str" action="" method="post">
        <div class="input-group">
            <p>ID</p>
            <input name="id" type="string" id="id" class="input" value="<%= item.id %>" disabled>
        </div>
        <div class="input-group">
            <input name="string" type="text" id="string" class="input" value="<%= item.string %>" required>
            <label for="string" class="input-label">String</label>
        </div>

        <div class="input-group">
            <input name="integer" type="number" id="integer" class="input" value="<%= item.integer %>" required>
            <label for="number" class="input-label">Integer</label>
        </div>

        <div class="input-group">
            <input name="float" type="number" step="any" id="float" class="input" value="<%= item.float %>" required>
            <label for="float" class="input-label">Float</label>
        </div>

        <div class="input-group">
            <p>Date</p>
            <input name="date" type="date" id="date" class="input" value="<%= item.date %>">
        </div>

        <div class="input-group">
            <p>Boolean</p>
            <select name="boolean" value="" id="boolean" class="input" value="<%= item.boolean %>" required>
                <option value="" selected disabled>-- Choose Boolean --</option>
                <option value=true <%=item.boolean==1 ? 'selected' : '' %>>True</option>
                <option value=false <%=item.boolean==0 ? 'selected' : '' %>>False</option>
            </select>
        </div>

        <div>
            <form action="" method="post">
                <br>
                <button type="submit" id="SaveButton">Update</button>
            </form>
            <a href="/" id="BackButton">Back</a>
        </div>
    </form>
    <br>
</body>

</html>